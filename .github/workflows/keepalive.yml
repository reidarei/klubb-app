name: Supabase keepalive

on:
  schedule:
    # Kjører hver fredag kl. 12:00 UTC — holder free tier aktiv
    - cron: '0 12 * * 5'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s -o /dev/null "${{ secrets.APP_URL }}"
